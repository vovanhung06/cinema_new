version: "3.8"

services:
  # mysql:
  #   image: mysql:8.0
  #   restart: always
  #   environment:
  #     MYSQL_DATABASE: cinema
  #     MYSQL_ROOT_PASSWORD: 123456
  #   ports:
  #     - "3307:3306"
  #   expose:
  #     - 3306

  backend:
    env_file: ../../backend/.env
    build:
      context: ../../backend
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      #DB_HOST: mysql
      #DB_PORT: 3306
      #DB_USER: root
      #DB_PASSWORD: 123456
      #DB_NAME: cinema
      DB_HOST: interchange.proxy.rlwy.net
      DB_PORT: 32798
      DB_USER: root
      DB_PASSWORD: AmmgfACUmsWypxiKNlrBsHYYmYMVUfhw
      DB_NAME: railway
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost:8080}
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:5173,http://localhost:5174,http://localhost:8080,http://127.0.0.1:8080}
    ports:
      - 3000:3000
